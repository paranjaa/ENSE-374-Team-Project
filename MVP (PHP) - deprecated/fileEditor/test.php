<?php

    // // Detect if a page was refreshed
    // $pageWasRefreshed = isset($_SERVER['HTTP_CACHE_CONTROL']) && $_SERVER['HTTP_CACHE_CONTROL'] === 'max-age=0';
 
    // if($pageWasRefreshed ) {
    // //do something because page was refreshed;
    // } else {
    // //do nothing;
    // }

?>